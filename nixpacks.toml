providers = ["python"]

[phases.setup]
nixPkgs = ["python310"]

[phases.install]
cmds = ["cd backend && python -m venv /opt/venv && /opt/venv/bin/pip install -r requirements.txt"]

[start]
cmd = "cd backend && /opt/venv/bin/uvicorn main:app --host 0.0.0.0 --port $PORT"
